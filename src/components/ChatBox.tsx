import React, { useEffect } from 'react';

const ChatBox: React.FC = () => {
  useEffect(() => {
    // This component is now empty as the functionality is in the HTML file
    // You can add any additional React-specific logic here if needed
  }, []);

  return null;
};

export default ChatBox;
